const query = '';
const term = '';
fetch(`...link/weather/${query}=${term}`);

// now it will fetch data to this particular query and term from out /weather? which in term fetch and filter data from original api